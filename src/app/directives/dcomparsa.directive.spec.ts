import { DcomparsaDirective } from './dcomparsa.directive';

describe('DcomparsaDirective', () => {
  it('should create an instance', () => {
    const directive = new DcomparsaDirective();
    expect(directive).toBeTruthy();
  });
});
